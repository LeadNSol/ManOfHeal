import 'package:flutter/material.dart';

class AdminLabUI extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(

        body: Container(
          child: Center(
            child: Text(
              'Coming Soon',
              style: TextStyle(fontSize: 20),
            ),
          ),
        ),
      ),
    );
  }
}
